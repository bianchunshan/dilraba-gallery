# Dilraba Gallery

主画廊：`index.html` + `images/`（205 张，按高级性感程度排序），元数据在 `new_photos.json`。

候选集：`candidate-500-v2/`（306 张，含 `candidate500_v2_manifest.json` 清单）。

筛选标准见 `CURATION_STANDARD.md`。

数据一致性要求：`new_photos.json` 条目数 = `images/` 图片数 = `index.html` 引用数。
